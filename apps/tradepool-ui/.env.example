# TradePool Package Configuration
# Copy this file to .env and fill in the values after deploying your contract

# Package ID after deployment
VITE_PACKAGE_ID=0x...

# Registry ID (shared object created on init)
VITE_REGISTRY_ID=0x...

# Admin Cap ID (transferred to deployer on init)
VITE_ADMIN_CAP_ID=0x...

# Momentum Version object ID
VITE_MOMENTUM_VERSION_ID=0x...

# Network (devnet, testnet, mainnet)
VITE_NETWORK=testnet
